{
  "name": "electron-tray",
  "version": "1.0.0",
  "description": "Electron app with system tray",
  "main": "dist/main.js",
  "scripts": {
    "start": "npm run build && ELECTRON_DISABLE_SANDBOX=1 electron --no-sandbox .",
    "dev": "npm run build && ELECTRON_DISABLE_SANDBOX=1 electron --no-sandbox .",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "build:electron": "electron-builder --publish never"
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^22.10.2",
    "electron-builder": "^24.13.3",
    "typescript": "^5.7.2",
    "electron": "^39.2.7"
  },
  "build": {
    "appId": "com.electron.timer-tray",
    "productName": "Timer Tray",
    "directories": {
      "output": "release"
    },
    "files": [
      "dist/**/*",
      "package.json",
      "node_modules/**/*"
    ],
    "win": {
      "target": [
        {
          "target": "zip",
          "arch": [
            "x64"
          ]
        }
      ]
    },
    "linux": {
      "target": [
        {
          "target": "zip",
          "arch": [
            "x64"
          ]
        }
      ],
      "category": "Utility"
    },
    "mac": {
      "target": [
        {
          "target": "zip",
          "arch": [
            "x64",
            "arm64"
          ]
        }
      ],
      "category": "public.app-category.utilities"
    }
  },
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.19.27",
    "canvas": "^3.2.0",
    "electron": "^28.0.0",
    "typescript": "^5.3.3"
  }
}
